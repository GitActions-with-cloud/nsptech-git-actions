name: CI
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print-greeting
        run: echo "heloo world!"
      - name: Print goodbye
        run: echo "Done   - bye!"
      - name: mult
        run: |
            echo "First output"
            echo "Second output"
        
